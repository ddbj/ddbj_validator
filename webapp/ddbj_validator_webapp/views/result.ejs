<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>ddbj validator alpha</title>

    <!-- Bootstrap -->
    <link href="stylesheets/validator.css" rel="stylesheet">
    <link href="stylesheets/fileinput.min.css" media="all" rel="stylesheet" type="text/css"/>
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <h1><a class="navbar-brand" href="#">DDBJ validator alpha</a></h1>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>
<div class="container result-summary">
    <h2>Error found in while checking this document</h2>
    <table class="result">
        <tbody>
            <tr>
                <th>Result:</th>
                <td>
                    <span class="error_info">
                        <% if(error_size != 0){ %>
                        <%= error_size %> Errors
                        <% } else { %>
                        No Errors Found
                        <% } %>
                    </span>
                </td>
            </tr>
            <tr>
                <th>File:</th>
                <td><%= original_file %></td>
            </tr>
            <% if(error_size != 0){ %>
            <tr>
                <th>Validation Method:</th>
                <td><%= errors[0].method %></td>
            </tr>
            <% } %>
        </tbody>
    </table>

</div>

<% if(error_size != 0){ %>
<div class="container result-group">
    <h2>Validation Output</h2>
    <% for(var i=0; i< errors.length; i++){ %>
    <h3>
        <span class="label label-danger error_level error">
            <%= errors[i].level %>
        </span>
        <%= errors[i].message %>
    </h3>
    <ul>
        <li>location: <%= errors[i].annotation[0].location %>, value: <%= errors[i].annotation[0].value %>, ID: <%= errors[i].id %></li>
    </ul>
    <% } %>
</div>
<% } %>

<footer id="footer">
    <div class="container">
        <p>Validator</p>
    </div>
</footer>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
<script src="javascripts/bootstrap.min.js"></script>


</body>
</html>